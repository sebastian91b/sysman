# Sysman
app for those who are afraid of terminal in Linux

Sysman is a simple tool written in Bash that helps less experienced Linux users perform basic administrative tasks without needing to use the terminal.

download both files, put in the same directory,
then run 'bash sysman.bash' or 'sudo bash sysman.bash' in terminal
